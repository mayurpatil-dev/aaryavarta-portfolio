export const metadata = {
  title: 'Mouse Poking Device Using LDR | Aaryavarta',
  description: 'A device using LDR sensors to detect and deter mice with a poking mechanism. Embedded automation project.',
};

export default function MousePokingDeviceLayout({ children }) {
  return children;
} 